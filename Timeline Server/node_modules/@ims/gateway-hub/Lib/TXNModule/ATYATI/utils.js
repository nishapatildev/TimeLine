/**
 * @param  {string} cbsTerminalId
 * @param  {string} bankCode
 */
function frameField42(cbsTerminalId, bankCode) {
  f42 = "000000000000";
  f42 = f42 + cbsTerminalId;
  f42 = f42.slice(-12);
  f42 = bankCode + f42;
  return f42;
}
/**
 * @param  {string} agentName
 * @param  {string} agentAddress
 * @param  {string} agentCity
 * @param  {string} agentState
 */
function frameField43(agentName, agentAddress, agentCity, agentState) {
  f43 = "                                        ";
  let AgentName_Address = (agentName + agentAddress + f43).slice(0, 23);
  let city = (agentCity + f43).slice(0, 13);
  let state = (agentState + f43).slice(0, 2);
  f43 = AgentName_Address + city + state + "IN";
  return f43;
}

module.exports = {
  frameField42: frameField42,
  frameField43: frameField43,
};
